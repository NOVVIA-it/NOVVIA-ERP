<UserControl x:Class="NovviaERP.WPF.Controls.TextmeldungPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NovviaERP.WPF.Controls">
    <UserControl.Resources>
        <local:TextmeldungTypToColorConverter x:Key="TypToBackgroundConverter" IsBackground="True"/>
        <local:TextmeldungTypToColorConverter x:Key="TypToBorderConverter" IsBackground="False"/>
    </UserControl.Resources>
    <Border x:Name="pnlMeldungen" Background="#FFF8E1" BorderBrush="#FFE082" BorderThickness="1"
            CornerRadius="4" Padding="10" Margin="0,0,0,10" Visibility="Collapsed">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="⚠" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtMeldungHeader" Text="Hinweise" FontWeight="SemiBold" FontSize="13"/>
                <Button x:Name="btnMeldungVerwalten" Content="Verwalten" Click="MeldungVerwalten_Click"
                        Padding="8,2" Margin="15,0,0,0" FontSize="11" Background="White"/>
            </StackPanel>

            <!-- Meldungsliste -->
            <ItemsControl x:Name="lstMeldungen" Grid.Row="1">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,3" Padding="8,5" CornerRadius="3"
                                Background="{Binding NTyp, Converter={StaticResource TypToBackgroundConverter}}"
                                BorderBrush="{Binding NTyp, Converter={StaticResource TypToBorderConverter}}"
                                BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding CTitel}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding CText}" TextWrapping="Wrap" Foreground="#555"
                                               Margin="0,3,0,0" FontSize="12"/>
                                </StackPanel>
                                <Button Grid.Column="1" Content="×" Click="MeldungEntfernen_Click"
                                        Tag="{Binding KEntityTextmeldung}"
                                        VerticalAlignment="Top" Padding="5,0" Background="Transparent"
                                        BorderThickness="0" Foreground="#999" Cursor="Hand" FontSize="14"
                                        ToolTip="Meldung von diesem Datensatz entfernen"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Keine Meldungen -->
            <TextBlock x:Name="txtKeineMeldungen" Grid.Row="2" Text="Keine Textmeldungen vorhanden."
                       Foreground="#888" FontStyle="Italic" Visibility="Collapsed"/>
        </Grid>
    </Border>
</UserControl>

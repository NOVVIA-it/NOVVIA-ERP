<Window x:Class="NovviaERP.WPF.Views.MSV3ResponseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MSV3 Bestellung - Ergebnis" Height="600" Width="800"
        WindowStartupLocation="CenterOwner" Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock x:Name="txtTitel" Text="MSV3 Bestellung" FontSize="20" FontWeight="Light"/>
            <TextBlock x:Name="txtSubtitel" Text="" Foreground="#666"/>
        </StackPanel>

        <!-- Positionen DataGrid -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="10">
            <DataGrid x:Name="dgPositionen" AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                      BorderThickness="0" Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="PZN" Binding="{Binding PZN}" Width="80"/>
                    <DataGridTextColumn Header="Artikel" Binding="{Binding ArtikelName}" Width="*"/>
                    <DataGridTemplateColumn Header="Menge" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Menge}" TextAlignment="Right" Margin="0,0,5,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Verfuegbar" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding VerfuegbareMenge}" TextAlignment="Right" Margin="0,0,5,0"
                                           Foreground="{Binding VerfuegbarFarbe}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Status" Binding="{Binding StatusCode}" Width="100"/>
                    <DataGridTextColumn Header="MHD" Binding="{Binding MHDText}" Width="85"/>
                    <DataGridTextColumn Header="Charge" Binding="{Binding ChargenNr}" Width="100"/>
                    <DataGridTextColumn Header="Lieferant" Binding="{Binding LieferantName}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Response XML (expandable) -->
        <Expander Grid.Row="2" Header="Response XML (Details)" Margin="0,10,0,0" IsExpanded="False">
            <Border Background="White" CornerRadius="4" Padding="10" MaxHeight="200">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <TextBox x:Name="txtResponseXml" FontFamily="Consolas" FontSize="11"
                             IsReadOnly="True" TextWrapping="Wrap" BorderThickness="0"
                             Background="White"/>
                </ScrollViewer>
            </Border>
        </Expander>

        <!-- Buttons -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="txtStatus" VerticalAlignment="Center" Foreground="#666"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="In Zwischenablage" Width="120" Height="35" Margin="0,0,10,0" Click="BtnCopy_Click"/>
                <Button Content="Schliessen" Width="100" Height="35" IsCancel="True" Click="BtnClose_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>

<UserControl x:Class="NovviaERP.WPF.Views.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <StackPanel Margin="50" VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="TEST VIEW (UserControl)" FontSize="32" FontWeight="Bold" Margin="0,0,0,30"/>

        <Button Content="KLICK MICH"
                Background="{DynamicResource PrimaryBrush}" Foreground="White"
                FontSize="24" Padding="30,20"
                Click="Button_Click"/>

        <TextBox x:Name="txtInput"
                 Text="Tippe hier..."
                 FontSize="18" Margin="0,20,0,0"
                 Padding="10"/>
    </StackPanel>
</UserControl>

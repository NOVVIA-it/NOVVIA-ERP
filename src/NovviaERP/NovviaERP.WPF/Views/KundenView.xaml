<UserControl x:Class="NovviaERP.WPF.Views.KundenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:NovviaERP.WPF.Converters"
             Background="#f0f2f5">
    <UserControl.Resources>
        <conv:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <Style x:Key="StatLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
        <Style x:Key="StatValue" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,2,0,8"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="400"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <!-- LINKE SEITE: Filter + Kundenliste -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Background="White" BorderBrush="#ddd" BorderThickness="0,0,0,1" Padding="15,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Kunden" FontSize="22" FontWeight="SemiBold" Foreground="#333"/>
                        <TextBlock x:Name="txtAnzahl" Text="" FontSize="13" Foreground="#888" VerticalAlignment="Center" Margin="12,0,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Nachricht" Padding="12,6" Margin="0,0,8,0" IsEnabled="False" x:Name="btnNachricht"/>
                        <Button Content="+ Neuer Kunde" Click="Neu_Click"
                                Background="#0078D4" Foreground="White" Padding="12,6" BorderThickness="0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Filter/Suche -->
            <Border Grid.Row="1" Background="White" BorderBrush="#ddd" BorderThickness="0,0,0,1" Padding="15,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtSuche" Height="30" Padding="8,5" VerticalContentAlignment="Center"
                             KeyDown="TxtSuche_KeyDown" ToolTip="Name, Kundennr, E-Mail, PLZ..."/>
                    <ComboBox x:Name="cboKundengruppe" Grid.Column="1" Width="140" Height="30" Margin="10,0,0,0"
                              DisplayMemberPath="CName" SelectionChanged="Filter_Changed">
                        <ComboBox.Items>
                            <ComboBoxItem Content="Alle Gruppen" IsSelected="True"/>
                        </ComboBox.Items>
                    </ComboBox>
                    <Button Grid.Column="2" Content="Suchen" Click="Suchen_Click" Margin="10,0,0,0"
                            Padding="12,5" Background="#0078D4" Foreground="White" BorderThickness="0"/>
                    <Button Grid.Column="3" Content="X" Click="FilterReset_Click" Margin="5,0,0,0"
                            Padding="8,5" ToolTip="Filter zuruecksetzen"/>
                </Grid>
            </Border>

            <!-- Kundenliste -->
            <DataGrid x:Name="dgKunden" Grid.Row="2" Margin="10,10,10,0"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      SelectionMode="Single" SelectionUnit="FullRow"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                      BorderThickness="1" BorderBrush="#ddd"
                      RowBackground="White" AlternatingRowBackground="#fafafa"
                      MouseDoubleClick="DG_DoubleClick" SelectionChanged="DG_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Kd-Nr" Binding="{Binding CKundenNr}" Width="80"/>
                    <DataGridTextColumn Header="Firma" Binding="{Binding CFirma}" Width="180"/>
                    <DataGridTextColumn Header="Vorname" Binding="{Binding CVorname}" Width="90" Visibility="Collapsed"/>
                    <DataGridTextColumn Header="Nachname" Binding="{Binding CName}" Width="100" Visibility="Collapsed"/>
                    <DataGridTextColumn Header="Firma/Name" Binding="{Binding Anzeigename}" Width="*"/>
                    <DataGridTextColumn Header="PLZ" Binding="{Binding CPLZ}" Width="60"/>
                    <DataGridTextColumn Header="Ort" Binding="{Binding COrt}" Width="100"/>
                    <DataGridTextColumn Header="Land" Binding="{Binding CISO}" Width="45"/>
                    <DataGridTextColumn Header="E-Mail" Binding="{Binding CMail}" Width="180" Visibility="Collapsed"/>
                    <DataGridTextColumn Header="Telefon" Binding="{Binding CTel}" Width="120" Visibility="Collapsed"/>
                    <DataGridTextColumn Header="Gruppe" Binding="{Binding Kundengruppe}" Width="90"/>
                    <DataGridTextColumn Header="Strasse" Binding="{Binding CStrasse}" Width="150" Visibility="Collapsed"/>
                    <DataGridTemplateColumn Header="" Width="30">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding CSperre}" Foreground="Red" FontWeight="Bold"
                                           Visibility="{Binding IstGesperrt, Converter={StaticResource BoolToColorConverter}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Statusleiste -->
            <Border Grid.Row="3" Background="#f8f8f8" Padding="15,8" BorderBrush="#ddd" BorderThickness="0,1,0,0">
                <Grid>
                    <TextBlock x:Name="txtStatus" Text="Bereit" Foreground="#666" FontSize="12"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Bearbeiten" Click="Bearbeiten_Click" Padding="10,4" IsEnabled="False" x:Name="btnBearbeiten"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- RECHTE SEITE: 360-Grad Kundendetails -->
        <Border Grid.Column="1" Background="White" BorderBrush="#ddd" BorderThickness="1,0,0,0">
            <Grid>
                <Grid x:Name="pnlDetails" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Kunde Header -->
                <Border Background="#0078D4" Padding="15">
                    <StackPanel>
                        <TextBlock x:Name="txtKundeName" Text="Kundenname" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                        <TextBlock x:Name="txtKundeNr" Text="Kd-Nr: 12345" FontSize="12" Foreground="#cce5ff" Margin="0,3,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Kontaktdaten -->
                <Border Grid.Row="1" Padding="15" BorderBrush="#eee" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="E-Mail" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="txtKundeMail" Text="-" FontSize="12" Margin="0,2,0,8"/>
                            <TextBlock Text="Telefon" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="txtKundeTel" Text="-" FontSize="12"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Adresse" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="txtKundeAdresse" Text="-" FontSize="12" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Statistiken -->
                <Border Grid.Row="2" Padding="15" BorderBrush="#eee" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Umsatz -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Umsatz gesamt" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="txtStatUmsatz" Text="0,00 EUR" Style="{StaticResource StatValue}" Foreground="#28a745"/>
                        </StackPanel>

                        <!-- Auftraege -->
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Auftraege" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="txtStatAuftraege" Text="0" Style="{StaticResource StatValue}"/>
                        </StackPanel>

                        <!-- Durchschnitt -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="O Warenkorb" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="txtStatDurchschnitt" Text="0,00 EUR" Style="{StaticResource StatValue}"/>
                        </StackPanel>

                        <!-- Offene Posten -->
                        <StackPanel Grid.Row="1" Grid.Column="0">
                            <TextBlock Text="Offene Posten" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="txtStatOffen" Text="0,00 EUR" Style="{StaticResource StatValue}" Foreground="#dc3545"/>
                        </StackPanel>

                        <!-- Retouren -->
                        <StackPanel Grid.Row="1" Grid.Column="1">
                            <TextBlock Text="Retouren" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="txtStatRetouren" Text="0" Style="{StaticResource StatValue}"/>
                        </StackPanel>

                        <!-- Kunde seit -->
                        <StackPanel Grid.Row="1" Grid.Column="2">
                            <TextBlock Text="Kunde seit" Style="{StaticResource StatLabel}"/>
                            <TextBlock x:Name="txtStatSeit" Text="-" Style="{StaticResource StatValue}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Tabs mit Details -->
                <TabControl Grid.Row="3" Margin="0" BorderThickness="0" Padding="0">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Padding" Value="12,6"/>
                            <Setter Property="FontSize" Value="12"/>
                        </Style>
                    </TabControl.Resources>

                    <!-- Tab: Auftraege -->
                    <TabItem Header="Auftraege">
                        <DataGrid x:Name="dgKundeAuftraege" AutoGenerateColumns="False" IsReadOnly="True"
                                  GridLinesVisibility="None" HeadersVisibility="Column" BorderThickness="0"
                                  RowBackground="White" AlternatingRowBackground="#fafafa" FontSize="11">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Nr" Width="70">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding CBestellNr}" Foreground="#0066CC" Cursor="Hand"
                                                       TextDecorations="Underline" MouseLeftButtonUp="AuftragNr_Click"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Datum" Binding="{Binding DErstellt, StringFormat=dd.MM.yy}" Width="65"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding CStatus}" Width="80"/>
                                <DataGridTextColumn Header="Betrag" Binding="{Binding GesamtBrutto, StringFormat=N2}" Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>

                    <!-- Tab: Rechnungen -->
                    <TabItem Header="Rechnungen">
                        <DataGrid x:Name="dgKundeRechnungen" AutoGenerateColumns="False" IsReadOnly="True"
                                  GridLinesVisibility="None" HeadersVisibility="Column" BorderThickness="0"
                                  RowBackground="White" AlternatingRowBackground="#fafafa" FontSize="11">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="Nr" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding CRechnungsNr}" Foreground="#0066CC" Cursor="Hand"
                                                       TextDecorations="Underline" MouseLeftButtonUp="RechnungNr_Click"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Datum" Binding="{Binding DRechnungsdatum, StringFormat=dd.MM.yy}" Width="65"/>
                                <DataGridTextColumn Header="Betrag" Binding="{Binding FBetragBrutto, StringFormat=N2}" Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Bezahlt" Binding="{Binding FBezahlt, StringFormat=N2}" Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>

                    <!-- Tab: Adressen -->
                    <TabItem Header="Adressen">
                        <DataGrid x:Name="dgKundeAdressen" AutoGenerateColumns="False" IsReadOnly="True"
                                  GridLinesVisibility="None" HeadersVisibility="Column" BorderThickness="0"
                                  RowBackground="White" AlternatingRowBackground="#fafafa" FontSize="11">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Typ" Binding="{Binding AdressTypText}" Width="90"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding CName}" Width="100"/>
                                <DataGridTextColumn Header="Strasse" Binding="{Binding CStrasse}" Width="*"/>
                                <DataGridTextColumn Header="Ort" Binding="{Binding COrt}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>

                    <!-- Tab: Historie -->
                    <TabItem Header="Historie">
                        <ListBox x:Name="lstHistorie" BorderThickness="0" FontSize="11">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,3">
                                        <TextBlock Text="{Binding Datum, StringFormat=dd.MM.yy}" Width="55" Foreground="#888"/>
                                        <TextBlock Text="{Binding Typ}" Width="80" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Beschreibung}" TextWrapping="NoWrap"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </TabItem>
                </TabControl>
            </Grid>

                <!-- Placeholder wenn kein Kunde ausgewaehlt -->
                <StackPanel x:Name="pnlNoSelection" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Kunde auswaehlen" FontSize="16" Foreground="#999" HorizontalAlignment="Center"/>
                    <TextBlock Text="Waehlen Sie einen Kunden aus der Liste" FontSize="12" Foreground="#bbb" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>

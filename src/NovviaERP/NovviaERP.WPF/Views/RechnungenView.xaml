<UserControl x:Class="NovviaERP.WPF.Views.RechnungenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#f5f5f5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="White" BorderBrush="#ddd" BorderThickness="0,0,0,1" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Rechnungen" FontSize="24" FontWeight="SemiBold" Foreground="#333"/>
                    <TextBlock x:Name="txtAnzahl" Text="" FontSize="14" Foreground="#888" VerticalAlignment="Center" Margin="15,0,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Aktualisieren" Click="Aktualisieren_Click"
                            Background="#0078D4" Foreground="White" Padding="15,8" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Filter -->
        <Border Grid.Row="1" Background="White" BorderBrush="#ddd" BorderThickness="0,0,0,1" Padding="20,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Status:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="cmbStatus" Width="150" Height="32" SelectionChanged="Status_Changed">
                    <ComboBoxItem Content="Alle" Tag="" IsSelected="True"/>
                    <ComboBoxItem Content="Offen" Tag="0"/>
                    <ComboBoxItem Content="Bezahlt" Tag="1"/>
                    <ComboBoxItem Content="Ueberfaellig" Tag="2"/>
                    <ComboBoxItem Content="Storniert" Tag="3"/>
                </ComboBox>

                <TextBlock Text="Suche:" VerticalAlignment="Center" Margin="20,0,8,0"/>
                <TextBox x:Name="txtSuche" Width="250" Height="32" Padding="10,5"
                         VerticalContentAlignment="Center" KeyDown="TxtSuche_KeyDown"
                         ToolTip="Rechnungsnr, Kunde, Auftrag"/>
                <Button Content="Suchen" Click="Suchen_Click" Margin="10,0" Padding="15,5" Background="#0078D4" Foreground="White" BorderThickness="0"/>

                <TextBlock Text="Zeitraum:" VerticalAlignment="Center" Margin="20,0,8,0"/>
                <ComboBox x:Name="cmbZeitraum" Width="130" Height="32" SelectionChanged="Zeitraum_Changed">
                    <ComboBoxItem Content="Alle" Tag="alle" IsSelected="True"/>
                    <ComboBoxItem Content="Heute" Tag="heute"/>
                    <ComboBoxItem Content="Gestern" Tag="gestern"/>
                    <ComboBoxItem Content="Diese Woche" Tag="diesewoche"/>
                    <ComboBoxItem Content="Letzte Woche" Tag="letztewoche"/>
                    <ComboBoxItem Content="Dieser Monat" Tag="diesermonat"/>
                    <ComboBoxItem Content="Letzter Monat" Tag="letztermonat"/>
                    <ComboBoxItem Content="Dieses Jahr" Tag="diesesjahr"/>
                    <ComboBoxItem Content="Eigener Zeitraum" Tag="eigener"/>
                </ComboBox>
                <TextBlock Text="Von:" VerticalAlignment="Center" Margin="10,0,8,0"/>
                <DatePicker x:Name="dpVon" Width="110" Height="32" SelectedDateChanged="Datum_Changed"/>
                <TextBlock Text="Bis:" VerticalAlignment="Center" Margin="5,0,8,0"/>
                <DatePicker x:Name="dpBis" Width="110" Height="32" SelectedDateChanged="Datum_Changed"/>
            </StackPanel>
        </Border>

        <!-- Rechnungsliste -->
        <DataGrid x:Name="dgRechnungen" Grid.Row="2" Margin="20,15,20,0"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" SelectionUnit="FullRow"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  BorderThickness="1" BorderBrush="#ddd"
                  RowBackground="White" AlternatingRowBackground="#fafafa"
                  MouseDoubleClick="DG_DoubleClick" SelectionChanged="DG_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Rechnungsnr." Binding="{Binding CRechnungsNr}" Width="130"/>
                <DataGridTextColumn Header="Datum" Binding="{Binding DErstellt, StringFormat=dd.MM.yyyy}" Width="90"/>
                <DataGridTextColumn Header="Kunde" Binding="{Binding KundeName}" Width="180"/>
                <DataGridTextColumn Header="Firma" Binding="{Binding KundeFirma}" Width="150"/>
                <DataGridTextColumn Header="Auftrag" Binding="{Binding CAuftragNr}" Width="100"/>
                <DataGridTextColumn Header="Faellig" Binding="{Binding DFaellig, StringFormat=dd.MM.yyyy}" Width="90"/>
                <DataGridTextColumn Header="Netto" Binding="{Binding FNetto, StringFormat=N2}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Brutto" Binding="{Binding FBrutto, StringFormat=N2}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Offen" Binding="{Binding Offen, StringFormat=N2}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Offen}" Value="0">
                                    <Setter Property="Foreground" Value="#28a745"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn Header="Status" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="3" Padding="6,2" HorizontalAlignment="Left">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#ffc107"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Bezahlt">
                                                <Setter Property="Background" Value="#28a745"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Ueberfaellig">
                                                <Setter Property="Background" Value="#dc3545"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Storniert">
                                                <Setter Property="Background" Value="#6c757d"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Status}" Foreground="White" FontSize="11"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Mahnstufe" Binding="{Binding NMahnstufe}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding NMahnstufe}" Value="0">
                                    <Setter Property="Foreground" Value="#ccc"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Statusleiste -->
        <Border Grid.Row="3" Background="#f0f0f0" Padding="20,10" BorderBrush="#ddd" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="txtStatus" Text="Bereit" Foreground="#666"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="btnOeffnen" Content="Oeffnen" Click="Oeffnen_Click" Padding="12,5" IsEnabled="False"/>
                    <Button x:Name="btnAuftrag" Content="Auftrag" Click="Auftrag_Click" Padding="12,5" Margin="10,0,0,0" IsEnabled="False"/>
                    <Button x:Name="btnZahlung" Content="Zahlung erfassen" Click="Zahlung_Click" Padding="12,5" Margin="10,0,0,0"
                            Background="#28a745" Foreground="White" BorderThickness="0" IsEnabled="False"/>
                    <Button x:Name="btnStorno" Content="Stornieren" Click="Storno_Click" Padding="12,5" Margin="10,0,0,0"
                            Background="#dc3545" Foreground="White" BorderThickness="0" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>

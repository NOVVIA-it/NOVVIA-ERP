<Window x:Class="NovviaERP.WPF.Views.EingangsrechnungDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Eingangsrechnung" Height="800" Width="1400"
        WindowStartupLocation="CenterScreen" Background="#F5F5F5">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <!-- Lieferantendaten -->
            <GroupBox Header="Lieferantendaten" Grid.Column="0" Margin="0,0,10,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="28"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Lieferant:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtLieferant" Grid.Row="0" Grid.Column="1" IsReadOnly="True" Background="#f0f0f0"/>
                    <TextBlock Text="Telefon:" Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    <TextBox x:Name="txtTelefon" Grid.Row="0" Grid.Column="3" IsReadOnly="True" Background="#f0f0f0"/>

                    <TextBlock Text="Strasse:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtStrasse" Grid.Row="1" Grid.Column="1" IsReadOnly="True" Background="#f0f0f0"/>
                    <TextBlock Text="E-Mail:" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    <TextBox x:Name="txtEmail" Grid.Row="1" Grid.Column="3" IsReadOnly="True" Background="#f0f0f0"/>

                    <TextBlock Text="PLZ/Ort:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                        <TextBox x:Name="txtPLZ" Width="60" IsReadOnly="True" Background="#f0f0f0"/>
                        <TextBox x:Name="txtOrt" Margin="5,0,0,0" IsReadOnly="True" Background="#f0f0f0" Width="150"/>
                    </StackPanel>
                    <TextBlock Text="Land:" Grid.Row="2" Grid.Column="2" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    <TextBox x:Name="txtLand" Grid.Row="2" Grid.Column="3" IsReadOnly="True" Background="#f0f0f0"/>

                    <TextBlock Text="Status:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
                    <ComboBox x:Name="cboStatus" Grid.Row="3" Grid.Column="1" Height="24">
                        <ComboBoxItem Content="Offen" Tag="0"/>
                        <ComboBoxItem Content="Freigegeben" Tag="5"/>
                        <ComboBoxItem Content="Gebucht" Tag="10"/>
                        <ComboBoxItem Content="Abgeschlossen" Tag="20"/>
                    </ComboBox>
                </Grid>
            </GroupBox>

            <!-- Enthaltene Bestellungen -->
            <GroupBox Header="Enthaltene Bestellungen" Grid.Column="1" Margin="0,0,10,10">
                <ListBox x:Name="lstBestellungen" Height="100"/>
            </GroupBox>

            <!-- Rechnungsdaten -->
            <GroupBox Header="Rechnungsdaten" Grid.Column="2" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="28"/>
                        <RowDefinition Height="28"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Fremdbelegnr.:" Grid.Row="0" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtFremdbelegnummer" Grid.Row="0" Grid.Column="1" IsReadOnly="True" Background="#f0f0f0" FontWeight="Bold"/>

                    <TextBlock Text="Eigene Nr.:" Grid.Row="1" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtEigeneNummer" Grid.Row="1" Grid.Column="1" IsReadOnly="True" Background="#f0f0f0"/>

                    <TextBlock Text="Belegdatum:" Grid.Row="2" VerticalAlignment="Center"/>
                    <DatePicker x:Name="dpBelegdatum" Grid.Row="2" Grid.Column="1"/>

                    <TextBlock Text="Zahlungsziel:" Grid.Row="3" VerticalAlignment="Center"/>
                    <DatePicker x:Name="dpZahlungsziel" Grid.Row="3" Grid.Column="1"/>
                </Grid>
            </GroupBox>
        </Grid>

        <!-- Row 1: Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <CheckBox x:Name="chkZurZahlungFreigeben" Content="Zur Zahlung freigeben" VerticalAlignment="Center"/>
            <TextBlock Text="Zahlbar bis:" VerticalAlignment="Center" Margin="20,0,5,0"/>
            <DatePicker x:Name="dpZahlbarBis" Width="120"/>
            <TextBlock Text="Hinweise:" VerticalAlignment="Center" Margin="20,0,5,0"/>
            <TextBox x:Name="txtHinweise" Width="300"/>
        </StackPanel>

        <!-- Row 2: Positionen -->
        <GroupBox Grid.Row="2" Header="Rechnungspositionen">
            <DataGrid x:Name="dgPositionen" AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" AlternatingRowBackground="#FFF0F0F0"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Artikelnr." Binding="{Binding CArtNr}" Width="100"/>
                    <DataGridTextColumn Header="Lief.Artikelnr." Binding="{Binding CLieferantenArtNr}" Width="100"/>
                    <DataGridTextColumn Header="Artikelname" Binding="{Binding CName}" Width="250"/>
                    <DataGridTextColumn Header="MwSt.%" Binding="{Binding FMwSt, StringFormat=N2}" Width="70">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/></Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Menge" Binding="{Binding FMenge, StringFormat=N2}" Width="80">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/></Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Einheit" Binding="{Binding CEinheit}" Width="60"/>
                    <DataGridTextColumn Header="Netto-EK" Binding="{Binding FEKNetto, StringFormat=N2}" Width="90">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/></Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Netto (ges.)" Binding="{Binding NettoGesamt, StringFormat=N2}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Brutto-EK" Binding="{Binding BruttoEK, StringFormat=N2}" Width="90">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Right"/></Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Brutto (ges.)" Binding="{Binding BruttoGesamt, StringFormat=N2}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Hinweis" Binding="{Binding CHinweis}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- Row 3: Footer -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Summen -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Positionen:" FontWeight="Bold"/>
                <TextBlock x:Name="txtAnzahlPositionen" Text="0" Margin="5,0,30,0"/>
                <TextBlock Text="Gesamtsumme (netto):" FontWeight="Bold"/>
                <TextBlock x:Name="txtSummeNetto" Text="0,00" Margin="5,0,30,0" FontWeight="Bold" Foreground="#0078D4"/>
                <TextBlock Text="Gesamtsumme (brutto):" FontWeight="Bold"/>
                <TextBlock x:Name="txtSummeBrutto" Text="0,00" Margin="5,0,0,0" FontWeight="Bold" FontSize="16" Foreground="#28a745"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Speichern" Width="100" Height="32" Margin="10,0" Click="Speichern_Click"
                        Background="#0078D4" Foreground="White" FontWeight="Bold"/>
                <Button Content="Schliessen" Width="100" Height="32" Click="Schliessen_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>

<Page x:Class="NovviaERP.WPF.Views.StammdatenPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Stammdaten" Background="#f0f2f5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Stammdaten" FontSize="28" FontWeight="Bold"/>
        
        <TabControl Grid.Row="1" Margin="0,15,0,0">
            <!-- Firmendaten -->
            <TabItem Header="🏢 Firma">
                <ScrollViewer><Border Style="{StaticResource CardStyle}" Margin="10">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="150"/><ColumnDefinition/><ColumnDefinition Width="150"/><ColumnDefinition/></Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/><RowDefinition Height="40"/><RowDefinition Height="40"/>
                            <RowDefinition Height="40"/><RowDefinition Height="40"/><RowDefinition Height="40"/>
                            <RowDefinition Height="40"/><RowDefinition Height="40"/><RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Firmenname:" VerticalAlignment="Center"/><TextBox x:Name="txtFirmaName" Grid.Column="1" Grid.ColumnSpan="3"/>
                        <TextBlock Text="Straße:" Grid.Row="1" VerticalAlignment="Center"/><TextBox x:Name="txtFirmaStrasse" Grid.Row="1" Grid.Column="1"/>
                        <TextBlock Text="PLZ/Ort:" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center"/><StackPanel Grid.Row="1" Grid.Column="3" Orientation="Horizontal"><TextBox x:Name="txtFirmaPLZ" Width="70"/><TextBox x:Name="txtFirmaOrt" Width="200" Margin="5,0,0,0"/></StackPanel>
                        <TextBlock Text="Telefon:" Grid.Row="2" VerticalAlignment="Center"/><TextBox x:Name="txtFirmaTel" Grid.Row="2" Grid.Column="1"/>
                        <TextBlock Text="E-Mail:" Grid.Row="2" Grid.Column="2" VerticalAlignment="Center"/><TextBox x:Name="txtFirmaMail" Grid.Row="2" Grid.Column="3"/>
                        <TextBlock Text="Website:" Grid.Row="3" VerticalAlignment="Center"/><TextBox x:Name="txtFirmaWWW" Grid.Row="3" Grid.Column="1"/>
                        <TextBlock Text="USt-ID:" Grid.Row="3" Grid.Column="2" VerticalAlignment="Center"/><TextBox x:Name="txtFirmaUStID" Grid.Row="3" Grid.Column="3"/>
                        <TextBlock Text="IBAN:" Grid.Row="4" VerticalAlignment="Center"/><TextBox x:Name="txtFirmaIBAN" Grid.Row="4" Grid.Column="1"/>
                        <TextBlock Text="BIC:" Grid.Row="4" Grid.Column="2" VerticalAlignment="Center"/><TextBox x:Name="txtFirmaBIC" Grid.Row="4" Grid.Column="3"/>
                        <TextBlock Text="Bank:" Grid.Row="5" VerticalAlignment="Center"/><TextBox x:Name="txtFirmaBank" Grid.Row="5" Grid.Column="1"/>
                        <TextBlock Text="Geschäftsführer:" Grid.Row="6" VerticalAlignment="Center"/><TextBox x:Name="txtFirmaGF" Grid.Row="6" Grid.Column="1"/>
                        <TextBlock Text="Amtsgericht:" Grid.Row="6" Grid.Column="2" VerticalAlignment="Center"/><TextBox x:Name="txtFirmaAG" Grid.Row="6" Grid.Column="3"/>
                        <TextBlock Text="Handelsregister:" Grid.Row="7" VerticalAlignment="Center"/><TextBox x:Name="txtFirmaHR" Grid.Row="7" Grid.Column="1"/>
                        <Button Content="💾 Speichern" Grid.Row="8" Grid.Column="3" HorizontalAlignment="Right" Width="120" Margin="0,15,0,0" Background="#0078D4" Foreground="White" Click="FirmaSpeichern_Click"/>
                    </Grid>
                </Border></ScrollViewer>
            </TabItem>
            
            <!-- Warenlager -->
            <TabItem Header="🏭 Lager">
                <Grid Margin="10"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="350"/></Grid.ColumnDefinitions>
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel><TextBlock Text="Warenlager" Style="{StaticResource HeaderText}"/>
                            <DataGrid x:Name="dgLager" Height="300" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="Lager_SelectionChanged">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                    <DataGridTextColumn Header="Kürzel" Binding="{Binding Kuerzel}" Width="80"/>
                                    <DataGridTextColumn Header="Ort" Binding="{Binding Ort}" Width="120"/>
                                    <DataGridCheckBoxColumn Header="Standard" Binding="{Binding IstStandard}" Width="70"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <Button Content="➕ Neues Lager" Margin="0,10,0,0" Click="NeuesLager_Click"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource CardStyle}" Grid.Column="1" Margin="10,0,0,0">
                        <StackPanel><TextBlock Text="Lagerplätze" Style="{StaticResource HeaderText}"/>
                            <DataGrid x:Name="dgLagerplaetze" Height="250" AutoGenerateColumns="False">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                    <DataGridTextColumn Header="Regal" Binding="{Binding Regal}" Width="60"/>
                                    <DataGridTextColumn Header="Fach" Binding="{Binding Fach}" Width="60"/>
                                    <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="100"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <Button Content="➕ Neuer Platz" Margin="0,10,0,0" Click="NeuerPlatz_Click"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
            
            <!-- Kategorien -->
            <TabItem Header="📁 Kategorien">
                <Grid Margin="10"><Grid.ColumnDefinitions><ColumnDefinition Width="300"/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel><TextBlock Text="Kategoriebaum" Style="{StaticResource HeaderText}"/>
                            <TreeView x:Name="tvKategorien" Height="400"/>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <Button Content="➕ Neu" Click="NeueKategorie_Click"/>
                                <Button Content="🗑 Löschen" Margin="10,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
            
            <!-- Kundengruppen -->
            <TabItem Header="👥 Kundengruppen">
                <Border Style="{StaticResource CardStyle}" Margin="10">
                    <StackPanel><TextBlock Text="Kundengruppen" Style="{StaticResource HeaderText}"/>
                        <DataGrid x:Name="dgKundengruppen" Height="300" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Rabatt %" Binding="{Binding Rabatt, StringFormat=N2}" Width="100"/>
                                <DataGridCheckBoxColumn Header="Nettopreise" Binding="{Binding NettoPreise}" Width="100"/>
                                <DataGridTextColumn Header="Standard" Binding="{Binding IstStandard}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Button Content="➕ Neue Kundengruppe" Margin="0,10,0,0" Click="NeueKundengruppe_Click"/>
                    </StackPanel>
                </Border>
            </TabItem>
            
            <!-- Versandarten -->
            <TabItem Header="🚚 Versandarten">
                <Border Style="{StaticResource CardStyle}" Margin="10">
                    <StackPanel><TextBlock Text="Versandarten" Style="{StaticResource HeaderText}"/>
                        <DataGrid x:Name="dgVersandarten" Height="300" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Anbieter" Binding="{Binding Anbieter}" Width="120"/>
                                <DataGridTextColumn Header="Preis" Binding="{Binding Preis, StringFormat=N2}" Width="80"/>
                                <DataGridTextColumn Header="Frei ab" Binding="{Binding VersandkostenfreiAb, StringFormat=N2}" Width="80"/>
                                <DataGridCheckBoxColumn Header="Aktiv" Binding="{Binding Aktiv}" Width="60"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Button Content="➕ Neue Versandart" Margin="0,10,0,0" Click="NeueVersandart_Click"/>
                    </StackPanel>
                </Border>
            </TabItem>
            
            <!-- Zahlungsarten -->
            <TabItem Header="💳 Zahlungsarten">
                <Border Style="{StaticResource CardStyle}" Margin="10">
                    <StackPanel><TextBlock Text="Zahlungsarten" Style="{StaticResource HeaderText}"/>
                        <DataGrid x:Name="dgZahlungsarten" Height="300" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Modul" Binding="{Binding ModulId}" Width="120"/>
                                <DataGridTextColumn Header="Aufpreis" Binding="{Binding Aufpreis, StringFormat=N2}" Width="80"/>
                                <DataGridTextColumn Header="Zahlungsziel" Binding="{Binding ZahlungszielTage}" Width="90"/>
                                <DataGridCheckBoxColumn Header="Aktiv" Binding="{Binding Aktiv}" Width="60"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </TabItem>
            
            <!-- Hersteller -->
            <TabItem Header="�icing Hersteller">
                <Border Style="{StaticResource CardStyle}" Margin="10">
                    <StackPanel><TextBlock Text="Hersteller / Marken" Style="{StaticResource HeaderText}"/>
                        <DataGrid x:Name="dgHersteller" Height="300" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Homepage" Binding="{Binding Homepage}" Width="200"/>
                                <DataGridTextColumn Header="Sortierung" Binding="{Binding Sortierung}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Button Content="➕ Neuer Hersteller" Margin="0,10,0,0" Click="NeuerHersteller_Click"/>
                    </StackPanel>
                </Border>
            </TabItem>
            
            <!-- Lieferanten -->
            <TabItem Header="📦 Lieferanten">
                <Border Style="{StaticResource CardStyle}" Margin="10">
                    <StackPanel><TextBlock Text="Lieferanten" Style="{StaticResource HeaderText}"/>
                        <DataGrid x:Name="dgLieferanten" Height="300" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Firma" Binding="{Binding Firma}" Width="*"/>
                                <DataGridTextColumn Header="Ort" Binding="{Binding Ort}" Width="120"/>
                                <DataGridTextColumn Header="E-Mail" Binding="{Binding Email}" Width="180"/>
                                <DataGridTextColumn Header="Lieferzeit" Binding="{Binding LieferzeitTage}" Width="80"/>
                                <DataGridCheckBoxColumn Header="Aktiv" Binding="{Binding Aktiv}" Width="60"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Button Content="➕ Neuer Lieferant" Margin="0,10,0,0" Click="NeuerLieferant_Click"/>
                    </StackPanel>
                </Border>
            </TabItem>
            
            <!-- Merkmale -->
            <TabItem Header="🏷️ Merkmale">
                <Grid Margin="10"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="350"/></Grid.ColumnDefinitions>
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel><TextBlock Text="Merkmale (Filter)" Style="{StaticResource HeaderText}"/>
                            <DataGrid x:Name="dgMerkmale" Height="300" AutoGenerateColumns="False" SelectionChanged="Merkmal_SelectionChanged">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                    <DataGridTextColumn Header="Typ" Binding="{Binding Typ}" Width="100"/>
                                    <DataGridCheckBoxColumn Header="Global" Binding="{Binding IstGlobal}" Width="60"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            <Button Content="➕ Neues Merkmal" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource CardStyle}" Grid.Column="1" Margin="10,0,0,0">
                        <StackPanel><TextBlock Text="Merkmalwerte" Style="{StaticResource HeaderText}"/>
                            <DataGrid x:Name="dgMerkmalWerte" Height="250" AutoGenerateColumns="False">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Wert" Binding="{Binding Wert}" Width="*"/>
                                    <DataGridTextColumn Header="Sortierung" Binding="{Binding Sortierung}" Width="80"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
            
            <!-- Mahnstufen -->
            <TabItem Header="⚠️ Mahnstufen">
                <Border Style="{StaticResource CardStyle}" Margin="10">
                    <StackPanel><TextBlock Text="Mahnstufen" Style="{StaticResource HeaderText}"/>
                        <DataGrid x:Name="dgMahnstufen" Height="250" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Stufe" Binding="{Binding Stufe}" Width="60"/>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Tage nach Fälligkeit" Binding="{Binding TageNachFaelligkeit}" Width="140"/>
                                <DataGridTextColumn Header="Gebühr" Binding="{Binding Gebuehr, StringFormat=N2}" Width="80"/>
                                <DataGridTextColumn Header="Zinsen %" Binding="{Binding ZinsProzent, StringFormat=N2}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </TabItem>
            
            <!-- Nummernkreise -->
            <TabItem Header="🔢 Nummernkreise">
                <Border Style="{StaticResource CardStyle}" Margin="10">
                    <StackPanel><TextBlock Text="Nummernkreise" Style="{StaticResource HeaderText}"/>
                        <DataGrid x:Name="dgNummernkreise" Height="300" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Prefix" Binding="{Binding Prefix}" Width="80"/>
                                <DataGridTextColumn Header="Aktuelle Nr." Binding="{Binding AktuelleNummer}" Width="100"/>
                                <DataGridTextColumn Header="Stellen" Binding="{Binding Stellen}" Width="70"/>
                                <DataGridCheckBoxColumn Header="Jahresabhängig" Binding="{Binding Jahresabhaengig}" Width="110"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</Page>

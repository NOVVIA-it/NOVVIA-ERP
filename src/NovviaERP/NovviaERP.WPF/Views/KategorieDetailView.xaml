<UserControl x:Class="NovviaERP.WPF.Views.KategorieDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#f5f5f5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="{DynamicResource PrimaryBrush}" Padding="15,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Content="Zurueck" Click="Zurueck_Click" Padding="10,5" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="txtTitel" Text="Kategorie" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock Text="Stammdaten und Artikel der Kategorie" FontSize="11" Foreground="#cce5ff"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <TabControl Grid.Row="1" Margin="15,10,15,0">
            <!-- Tab Stammdaten -->
            <TabItem Header="Stammdaten">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="15">
                    <StackPanel MaxWidth="800">
                        <!-- Allgemein -->
                        <GroupBox Header="Allgemein" Margin="0,0,0,15">
                            <Grid Margin="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="32"/>
                                    <RowDefinition Height="32"/>
                                    <RowDefinition Height="100"/>
                                    <RowDefinition Height="32"/>
                                    <RowDefinition Height="32"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="Name:" VerticalAlignment="Center"/>
                                <TextBox x:Name="txtName" Grid.Column="1" Padding="8,4" FontSize="14"/>

                                <TextBlock Text="Oberkategorie:" Grid.Row="1" VerticalAlignment="Center"/>
                                <ComboBox x:Name="cmbOberkategorie" Grid.Row="1" Grid.Column="1" Padding="8,4"/>

                                <TextBlock Text="Beschreibung:" Grid.Row="2" VerticalAlignment="Top" Margin="0,5,0,0"/>
                                <TextBox x:Name="txtBeschreibung" Grid.Row="2" Grid.Column="1" Padding="8,4"
                                         AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

                                <TextBlock Text="Sortierung:" Grid.Row="3" VerticalAlignment="Center"/>
                                <TextBox x:Name="txtSort" Grid.Row="3" Grid.Column="1" Padding="8,4" Width="80" HorizontalAlignment="Left"/>

                                <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal">
                                    <CheckBox x:Name="chkAktiv" Content="Aktiv" Margin="0,0,20,0" VerticalAlignment="Center"/>
                                    <CheckBox x:Name="chkInternet" Content="Im Internet sichtbar" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </GroupBox>

                        <!-- SEO -->
                        <GroupBox Header="SEO / Metadaten" Margin="0,0,0,15">
                            <Grid Margin="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="32"/>
                                    <RowDefinition Height="32"/>
                                    <RowDefinition Height="80"/>
                                    <RowDefinition Height="80"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="URL-Pfad:" VerticalAlignment="Center"/>
                                <TextBox x:Name="txtUrlPfad" Grid.Column="1" Padding="8,4"/>

                                <TextBlock Text="Title-Tag:" Grid.Row="1" VerticalAlignment="Center"/>
                                <TextBox x:Name="txtTitleTag" Grid.Row="1" Grid.Column="1" Padding="8,4"/>

                                <TextBlock Text="Meta-Description:" Grid.Row="2" VerticalAlignment="Top" Margin="0,5,0,0"/>
                                <TextBox x:Name="txtMetaDescription" Grid.Row="2" Grid.Column="1" Padding="8,4"
                                         AcceptsReturn="True" TextWrapping="Wrap"/>

                                <TextBlock Text="Keywords:" Grid.Row="3" VerticalAlignment="Top" Margin="0,5,0,0"/>
                                <TextBox x:Name="txtMetaKeywords" Grid.Row="3" Grid.Column="1" Padding="8,4"
                                         AcceptsReturn="True" TextWrapping="Wrap"/>
                            </Grid>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab Artikel -->
            <TabItem Header="Artikel">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock x:Name="txtArtikelAnzahl" Text="0 Artikel" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <Button Content="+ Artikel hinzufuegen" Click="ArtikelHinzufuegen_Click"
                                Padding="10,5" Margin="20,0,0,0"/>
                        <Button Content="- Artikel entfernen" Click="ArtikelEntfernen_Click"
                                Padding="10,5" Margin="10,0,0,0"/>
                    </StackPanel>

                    <DataGrid x:Name="dgArtikel" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                              BorderThickness="1" BorderBrush="#ddd">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Art.-Nr." Binding="{Binding CArtNr}" Width="120"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding CName}" Width="*"/>
                            <DataGridTextColumn Header="VK Netto" Binding="{Binding FVKNetto, StringFormat=N2}" Width="100"/>
                            <DataGridTextColumn Header="Bestand" Binding="{Binding FLagerbestand}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Tab Unterkategorien -->
            <TabItem Header="Unterkategorien">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock x:Name="txtSubAnzahl" Text="0 Unterkategorien" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <Button Content="+ Neue Unterkategorie" Click="NeueUnterkategorie_Click"
                                Padding="10,5" Margin="20,0,0,0"/>
                    </StackPanel>

                    <DataGrid x:Name="dgUnterkategorien" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                              BorderThickness="1" BorderBrush="#ddd" MouseDoubleClick="DgUnterkategorien_DoubleClick">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding CName}" Width="*"/>
                            <DataGridTextColumn Header="Artikel" Binding="{Binding ArtikelAnzahl}" Width="80"/>
                            <DataGridTextColumn Header="Sub-Kat." Binding="{Binding SubkategorienAnzahl}" Width="80"/>
                            <DataGridCheckBoxColumn Header="Aktiv" Binding="{Binding CAktiv}" Width="60"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Footer -->
        <Border Grid.Row="2" Background="White" BorderBrush="#ddd" BorderThickness="0,1,0,0" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="txtStatus" Foreground="#666" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Speichern" Click="Speichern_Click" Padding="15,8"
                            Background="{DynamicResource PrimaryBrush}" Foreground="White" BorderThickness="0" Margin="0,0,10,0"/>
                    <Button Content="Abbrechen" Click="Zurueck_Click" Padding="15,8"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>

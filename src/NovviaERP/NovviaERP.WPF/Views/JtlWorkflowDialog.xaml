<Window x:Class="NovviaERP.WPF.Views.JtlWorkflowDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="JTL Workflow bearbeiten" Height="650" Width="800"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Grunddaten -->
        <GroupBox Grid.Row="0" Header="Grunddaten" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="150"/>
                </Grid.ColumnDefinitions>

                <!-- Name -->
                <TextBlock Grid.Row="0" Text="Name:" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3" x:Name="txtName" Margin="0,0,0,10"/>

                <!-- Objekt -->
                <TextBlock Grid.Row="1" Text="Objekt:" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="1" Grid.Column="1" x:Name="cmbObjekt" Margin="0,0,10,10" SelectionChanged="Objekt_Changed">
                    <ComboBoxItem Content="Kunde" Tag="5"/>
                    <ComboBoxItem Content="Auftrag" Tag="6"/>
                    <ComboBoxItem Content="Lieferschein" Tag="7"/>
                    <ComboBoxItem Content="Rechnung" Tag="8"/>
                </ComboBox>

                <!-- Typ -->
                <TextBlock Grid.Row="1" Grid.Column="2" Text="Typ:" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="1" Grid.Column="3" x:Name="cmbTyp" Margin="0,0,0,10">
                    <ComboBoxItem Content="Standard" Tag="0" IsSelected="True"/>
                    <ComboBoxItem Content="Manuell" Tag="1"/>
                    <ComboBoxItem Content="Zeitgesteuert" Tag="2"/>
                </ComboBox>

                <!-- Event -->
                <TextBlock Grid.Row="2" Text="Ereignis:" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" x:Name="cmbEvent" DisplayMemberPath="CDisplayName" SelectedValuePath="NEvent"/>
            </Grid>
        </GroupBox>

        <!-- Aktionen -->
        <GroupBox Grid.Row="1" Header="Aktionen" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="120"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <Button Content="+ SQL Aktion" Click="AddSqlAktion_Click" Padding="10,5" Margin="0,0,10,0"/>
                    <Button Content="+ Wert setzen" Click="AddWertSetzenAktion_Click" Padding="10,5" Margin="0,0,10,0"/>
                    <Button Content="+ Datei schreiben" Click="AddDateiAktion_Click" Padding="10,5" Margin="0,0,10,0"/>
                    <Button x:Name="btnAktionLoeschen" Content="Loeschen" Click="AktionLoeschen_Click" Padding="10,5" IsEnabled="False"/>
                </StackPanel>

                <DataGrid Grid.Row="1" x:Name="dgAktionen" AutoGenerateColumns="False" SelectionChanged="Aktion_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Pos" Binding="{Binding NPos}" Width="50"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding CName}" Width="200"/>
                        <DataGridTextColumn Header="Typ" Binding="{Binding AktionTyp}" Width="120"/>
                        <DataGridTextColumn Header="Parameter" Binding="{Binding ParameterPreview}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- Bedingungen -->
        <GroupBox Grid.Row="2" Header="Bedingungen (optional)" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="100"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <Button Content="+ Bedingung" Click="AddBedingung_Click" Padding="10,5" Margin="0,0,10,0"/>
                    <Button x:Name="btnBedingungLoeschen" Content="Loeschen" Click="BedingungLoeschen_Click" Padding="10,5" IsEnabled="False"/>
                </StackPanel>

                <DataGrid Grid.Row="1" x:Name="dgBedingungen" AutoGenerateColumns="False" SelectionChanged="Bedingung_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Pos" Binding="{Binding NPos}" Width="50"/>
                        <DataGridTextColumn Header="Eigenschaft" Binding="{Binding Eigenschaft}" Width="200"/>
                        <DataGridComboBoxColumn Header="Operator" SelectedValueBinding="{Binding NOperator}" Width="80">
                            <DataGridComboBoxColumn.ItemsSource>
                                <x:Array Type="{x:Type ComboBoxItem}">
                                    <ComboBoxItem Content="=" Tag="0"/>
                                    <ComboBoxItem Content="!=" Tag="1"/>
                                    <ComboBoxItem Content=">" Tag="2"/>
                                    <ComboBoxItem Content="&lt;" Tag="3"/>
                                </x:Array>
                            </DataGridComboBoxColumn.ItemsSource>
                        </DataGridComboBoxColumn>
                        <DataGridTextColumn Header="Wert" Binding="{Binding CVergleichswert}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- Aktion Details -->
        <GroupBox Grid.Row="3" Header="Aktion bearbeiten" x:Name="grpAktionDetails" Visibility="Collapsed">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Name:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" x:Name="txtAktionName" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="1" Text="SQL/Wert:" VerticalAlignment="Top"/>
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="txtAktionParameter" AcceptsReturn="True"
                         TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" Margin="0,0,0,10"/>

                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Aktion speichern" Click="AktionSpeichern_Click" Padding="10,5"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Speichern" Click="Speichern_Click" Width="100" Padding="10,8"
                    Background="#4CAF50" Foreground="White" BorderThickness="0" Margin="0,0,10,0"/>
            <Button Content="Abbrechen" Click="Abbrechen_Click" Width="100" Padding="10,8"/>
        </StackPanel>
    </Grid>
</Window>

<Window x:Class="NovviaERP.WPF.Views.AdresseAuswahlDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adresse auswaehlen" Height="450" Width="600"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        Background="#f5f5f5">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Margin="0,0,0,10">
            <TextBlock x:Name="txtHeader" Text="Adresse auswaehlen" FontSize="16" FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryBrush}"/>
            <TextBlock x:Name="txtSubHeader" Text="Waehlen Sie eine Adresse aus der Liste"
                       FontSize="11" Foreground="#666" Margin="0,3,0,0"/>
        </StackPanel>

        <!-- Adressen Liste -->
        <Border Grid.Row="1" Background="White" BorderBrush="#ddd" BorderThickness="1">
            <ListBox x:Name="lstAdressen" SelectionMode="Single" BorderThickness="0"
                     MouseDoubleClick="LstAdressen_DoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10" BorderBrush="#eee" BorderThickness="0,0,0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="{DynamicResource PrimaryBrush}" Width="40" Height="40" CornerRadius="20"
                                        VerticalAlignment="Top" Margin="0,0,12,0">
                                    <TextBlock Text="{Binding Initiale}" Foreground="White" FontWeight="Bold"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Titel}" FontWeight="SemiBold" FontSize="13"/>
                                    <TextBlock Text="{Binding Zeile1}" Foreground="#666" FontSize="11"/>
                                    <TextBlock Text="{Binding Zeile2}" Foreground="#666" FontSize="11"/>
                                    <TextBlock Text="{Binding Typ}" Foreground="{DynamicResource PrimaryBrush}" FontSize="10"
                                               FontStyle="Italic" Margin="0,3,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Neue Adresse Button -->
        <Button Grid.Row="2" Content="+ Neue Adresse erfassen" Click="NeueAdresse_Click"
                HorizontalAlignment="Left" Padding="10,5" Margin="0,10,0,0"
                Background="Transparent" BorderBrush="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource PrimaryBrush}"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Uebernehmen" Click="Uebernehmen_Click" Padding="20,8" Margin="0,0,10,0"
                    Background="{DynamicResource PrimaryBrush}" Foreground="White" BorderThickness="0"/>
            <Button Content="Abbrechen" Click="Abbrechen_Click" Padding="15,8" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

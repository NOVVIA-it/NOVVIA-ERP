<Page x:Class="NovviaERP.WPF.Views.ImportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Import" Background="#f5f5f5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="White" BorderBrush="#ddd" BorderThickness="0,0,0,1" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Daten importieren" FontSize="24" FontWeight="SemiBold" Foreground="#333"/>
                    <TextBlock Text="Auftraege und Bestellungen aus Excel/CSV importieren" FontSize="12" Foreground="#666" Margin="0,3,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Linke Spalte: Konfiguration -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Schritt 1: Datei -->
                    <Border Background="White" BorderBrush="#ddd" BorderThickness="1" Padding="15" CornerRadius="4" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="1. Datei auswaehlen" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="txtDateiPfad" IsReadOnly="True" Height="28"
                                         VerticalContentAlignment="Center" Padding="8,0"/>
                                <Button Grid.Column="1" Content="..." Width="35" Height="28" Margin="5,0,0,0"
                                        Click="DateiAuswaehlen_Click"/>
                            </Grid>
                            <TextBlock x:Name="txtDateiInfo" Text="" Foreground="#666" FontSize="11" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Schritt 2: Import-Typ -->
                    <Border Background="White" BorderBrush="#ddd" BorderThickness="1" Padding="15" CornerRadius="4" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="2. Import-Typ" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>
                            <RadioButton x:Name="rbAuftrag" Content="Auftraege (Verkauf)" IsChecked="True"
                                         GroupName="ImportTyp" Margin="0,0,0,8" Checked="ImportTyp_Changed"/>
                            <RadioButton x:Name="rbBestellung" Content="Lieferantenbestellungen (Einkauf)"
                                         GroupName="ImportTyp" Checked="ImportTyp_Changed"/>
                        </StackPanel>
                    </Border>

                    <!-- Schritt 3: Format-Optionen -->
                    <Border Background="White" BorderBrush="#ddd" BorderThickness="1" Padding="15" CornerRadius="4" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="3. Format-Optionen" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="32"/>
                                    <RowDefinition Height="32"/>
                                    <RowDefinition Height="32"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="CSV-Trennzeichen:" VerticalAlignment="Center"/>
                                <ComboBox x:Name="cmbTrennzeichen" Grid.Column="1" Height="26" SelectedIndex="0">
                                    <ComboBoxItem Content="Semikolon (;)" Tag=";"/>
                                    <ComboBoxItem Content="Komma (,)" Tag=","/>
                                    <ComboBoxItem Content="Tab" Tag="&#x9;"/>
                                    <ComboBoxItem Content="Pipe (|)" Tag="|"/>
                                </ComboBox>

                                <TextBlock Text="Encoding:" Grid.Row="1" VerticalAlignment="Center"/>
                                <ComboBox x:Name="cmbEncoding" Grid.Row="1" Grid.Column="1" Height="26" SelectedIndex="0">
                                    <ComboBoxItem Content="UTF-8" Tag="utf-8"/>
                                    <ComboBoxItem Content="Windows-1252" Tag="windows-1252"/>
                                    <ComboBoxItem Content="ISO-8859-1" Tag="iso-8859-1"/>
                                </ComboBox>

                                <CheckBox x:Name="chkHeaderZeile" Grid.Row="2" Grid.ColumnSpan="2"
                                          Content="Erste Zeile ist Ueberschrift" IsChecked="True" VerticalAlignment="Center"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Schritt 4: Spaltenzuordnung -->
                    <Border Background="White" BorderBrush="#ddd" BorderThickness="1" Padding="15" CornerRadius="4">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="4. Spaltenzuordnung" FontWeight="SemiBold" FontSize="14"/>
                                <Button Grid.Column="1" Content="Auto-Erkennung" Click="AutoErkennung_Click"
                                        Padding="8,3" FontSize="11"/>
                            </Grid>
                            <ScrollViewer MaxHeight="300" Margin="0,12,0,0" VerticalScrollBarVisibility="Auto">
                                <StackPanel x:Name="pnlSpaltenzuordnung">
                                    <TextBlock Text="Bitte zuerst eine Datei auswaehlen" Foreground="#888" FontStyle="Italic"/>
                                </StackPanel>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Rechte Spalte: Vorschau -->
            <Border Grid.Column="2" Background="White" BorderBrush="#ddd" BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Background="#f8f8f8" Padding="15" BorderBrush="#ddd" BorderThickness="0,0,0,1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Vorschau" FontWeight="SemiBold" FontSize="14"/>
                                <TextBlock x:Name="txtVorschauInfo" Text="Keine Datei geladen" Foreground="#666" FontSize="11"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="Aktualisieren" Click="VorschauAktualisieren_Click"
                                    Padding="10,5"/>
                        </Grid>
                    </Border>

                    <DataGrid x:Name="dgVorschau" Grid.Row="1" AutoGenerateColumns="True" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="All"
                              RowBackground="White" AlternatingRowBackground="#fafafa"
                              CanUserAddRows="False" CanUserDeleteRows="False"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="White" BorderBrush="#ddd" BorderThickness="0,1,0,0" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="txtStatus" Text="Bereit" Foreground="#666" VerticalAlignment="Center"/>
                    <ProgressBar x:Name="pbImport" Width="200" Height="18" Margin="15,0,0,0"
                                 Visibility="Collapsed" IsIndeterminate="True"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Importieren" Click="Importieren_Click" Padding="20,10"
                            Background="#28a745" Foreground="White" BorderThickness="0"
                            x:Name="btnImportieren" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
